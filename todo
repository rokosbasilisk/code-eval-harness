create a docker volume for data and test_programs 
mount the volume and run the programs inside the container 
send the output accuracy value as a return value (do proper checking)
delete all the test_programs 

